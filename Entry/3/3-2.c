#define __STDC_LIMIT_MACROS
#include <stdio.h>
#include <stdint.h>
#include <time.h>
#include <limits.h>// define INT_MAX INT_MIN
#include <string.h>

int arr[8][8];


int main(int argc, char * argv[])
{

    int n = 0;
    scanf("%d", &n);
    int sum = n * n;
    
    int x = n -1;
    int y = 0;

    for (int i = 1; i<=sum; ++i)    
    {
    }

    return 0;
}
